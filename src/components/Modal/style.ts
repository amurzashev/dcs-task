import { Styles } from "react-modal";

const styles: Styles = {
  content: {
    padding: 0,
    height: 400,
    width: 300,
    position: "relative",
    top: "initial",
    bottom: "initial",
    left: "initial",
    right: "initial",
    marginTop: 40,
  },
  overlay: {
    display: "flex",
    justifyContent: "center",
  },
};

export default styles;
