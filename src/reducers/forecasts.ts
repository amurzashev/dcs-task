import { Forecast } from "types";

const initialState: Forecast[] = [];

// temp
export default (state = initialState, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};
